import { useEffect, useMemo, useState } from 'react';
import { api } from '@appdeploy/client';
import { ArrowDownLeft, ArrowUpRight, Bell, Check, ChevronRight, CreditCard, Eye, EyeOff, FileText, Globe2, Home, Landmark, Menu, MoreHorizontal, Plus, ReceiptText, Search, Send, Settings, ShieldCheck, Smartphone, Sparkles, Tv, Wallet, Wifi, X, Zap } from 'lucide-react';
import ServiceFlow, { ServiceKind, ServiceRecord } from './ServiceFlow';
import AuthScreen from './AuthScreen';
type Status = 'completed' | 'processing' | 'failed' | 'reversed';
type Tx = { id:string; title:string; subtitle:string; amount:string; positive?:boolean; date:string; status:Status; category?:string };
const demoTx:Tx[]=[{id:'1',title:'Transfer received',subtitle:'From Chinedu Okafor',amount:'+₦185,000',positive:true,date:'Today, 10:42',status:'completed',category:'Money in'},{id:'2',title:'Netflix',subtitle:'Card •••• 4821',amount:'-₦7,100',date:'Yesterday, 18:20',status:'completed',category:'Money out'},{id:'3',title:'Airtime',subtitle:'MTN Nigeria',amount:'-₦5,000',date:'Aug 31, 13:08',status:'completed',category:'Money out'},{id:'4',title:'USD conversion',subtitle:'USD wallet',amount:'-$100',date:'Aug 30, 09:15',status:'processing',category:'Wallet'}];
const services=[{label:'Airtime',icon:Smartphone,message:'Airtime is ready for a provider connection.'},{label:'Data',icon:Wifi,message:'Data bundles are ready for a provider connection.'},{label:'Electricity',icon:Zap,message:'Electricity bill payment is ready for a provider connection.'},{label:'TV',icon:Tv,message:'TV subscriptions are ready for a provider connection.'},{label:'International',icon:Globe2,message:'International transfers are available as a sandbox flow.'},{label:'Multi-currency',icon:Wallet,message:'Multi-currency wallets are available in sandbox mode.'}];
function App(){const [tab,setTab]=useState('Home'),[balanceVisible,setBalanceVisible]=useState(true),[currency,setCurrency]=useState('NGN'),[balance,setBalance]=useState(1247850),[txs,setTxs]=useState<Tx[]>(demoTx),[showSend,setShowSend]=useState(false),[showMore,setShowMore]=useState(false),[showWalletTools,setShowWalletTools]=useState(false),[selectedTx,setSelectedTx]=useState<Tx|null>(null),[recipient,setRecipient]=useState(''),[amount,setAmount]=useState(''),[transferStep,setTransferStep]=useState<'form'|'review'|'result'>('form'),[transferStatus,setTransferStatus]=useState<Status>('completed'),[notice,setNotice]=useState(''),[search,setSearch]=useState(''),[activityFilter,setActivityFilter]=useState('All'),[cardFrozen,setCardFrozen]=useState(false),[serviceKind,setServiceKind]=useState<ServiceKind|null>(null),[session,setSession]=useState<{token:string;user:{id:string;fullName:string;email:string;phone:string}}|null>(()=>{try{const saved=localStorage.getItem('bhs_session');return saved?JSON.parse(saved):null;}catch{return null;}});
useEffect(()=>{api.get('/api/health').catch(()=>undefined);if(session?.token)api.post('/api/auth/me',{sessionToken:session.token}).then(r=>{const next={...session,user:r.data.user};localStorage.setItem('bhs_session',JSON.stringify(next));setSession(next);}).catch(()=>{localStorage.removeItem('bhs_session');setSession(null);});},[]);
const formatted=useMemo(()=>currency==='NGN'?`₦${balance.toLocaleString('en-NG')}.00`:`${(balance/1550).toFixed(2)}`,[balance,currency]);
const visibleTxs=useMemo(()=>txs.filter(tx=>`${tx.title} ${tx.subtitle}`.toLowerCase().includes(search.toLowerCase())&&(activityFilter==='All'||tx.category===activityFilter)),[txs,search,activityFilter]);
function openSend(){setNotice('');setTransferStep('form');setShowSend(true);}function closeSend(){setShowSend(false);setRecipient('');setAmount('');setNotice('');setTransferStep('form');}function reviewTransfer(){const value=Number(amount.replace(/,/g,''));if(!recipient.trim()||!Number.isFinite(value)||value<=0)return setNotice('Enter a recipient and a valid amount.');if(currency==='NGN'&&value>balance)return setNotice('Insufficient balance for this demo transfer.');setNotice('');setTransferStep('review');}
function handleAuthenticated(next:{token:string;user:{id:string;fullName:string;email:string;phone:string}}){localStorage.setItem('bhs_session',JSON.stringify(next));setSession(next);}async function signOut(){if(session?.token)await api.post('/api/auth/logout',{sessionToken:session.token}).catch(()=>undefined);localStorage.removeItem('bhs_session');setSession(null);setShowMore(false);}async function confirmTransfer(){const value=Number(amount.replace(/,/g,''));let finalStatus:Status='completed';try{await api.post('/api/transfers',{recipient:recipient.trim(),amount:value,currency,sessionToken:session?.token});}catch{finalStatus='processing';}setTransferStatus(finalStatus);setBalance(b=>currency==='NGN'?b-value:b);setTxs(items=>[{id:crypto.randomUUID(),title:'Transfer sent',subtitle:recipient.trim(),amount:`-${currency==='NGN'?'₦':'$'}${value.toLocaleString()}`,date:'Just now',status:finalStatus,category:'Money out'},...items]);setTransferStep('result');}
function openService(kind:ServiceKind){setNotice('');setServiceKind(kind);}function completeService(record:ServiceRecord){setTxs(items=>[{id:crypto.randomUUID(),title:record.title,subtitle:record.subtitle,amount:record.amount,date:'Just now',status:'processing',category:'Money out'},...items]);}
if(!session)return <AuthScreen onAuthenticated={handleAuthenticated}/>;const primaryServices=services.slice(0,4);
return <div className="shell"><header className="topbar"><div className="brand"><span className="brand-mark">BH</span><span>BH'S</span></div><div className="top-actions"><button aria-label="Notifications" onClick={()=>setNotice('You have 2 security notifications.')}><Bell size={19}/><i/></button><button aria-label="Open menu" onClick={()=>setShowMore(true)}><Menu size={21}/></button></div></header><main>
{tab==='Home'&&<><section className="hero"><div className="eyebrow"><span>AVAILABLE BALANCE</span><button aria-label="Toggle balance visibility" onClick={()=>setBalanceVisible(v=>!v)}>{balanceVisible?<Eye size={17}/>:<EyeOff size={17}/>}</button></div><div className="balance">{balanceVisible?formatted:'••••••••'}</div><div className="balance-row"><span>Primary wallet</span><button onClick={()=>setCurrency(c=>c==='NGN'?'USD':'NGN')}>{currency}<ChevronRight size={14}/></button></div><div className="hero-actions"><button onClick={openSend}><Send size={17}/> Send money</button><button onClick={()=>setNotice('Your BH\'S receiving details are ready to share.')}><ArrowDownLeft size={17}/> Receive</button></div></section><section className="quick-grid">{primaryServices.map(({label,icon:Icon})=><button key={label} onClick={()=>openService(label as ServiceKind)}><span><Icon size={20}/></span>{label}</button>)}</section><section className="section service-strip"><div className="section-head"><div><p className="kicker">EVERYDAY MONEY</p><h2>Everything in one place.</h2></div><Landmark size={23}/></div><div className="service-list">{services.slice(4).map(({label,icon:Icon})=><button key={label} onClick={()=>openService(label as ServiceKind)}><span className="service-icon"><Icon size={19}/></span><span><strong>{label}</strong><small>{label==='International'?'Send across borders':'Hold & convert'}</small></span><ChevronRight size={17}/></button>)}</div></section><section className="section promo"><div><p className="kicker">BH'S SECURITY</p><h2>Move money with confidence.</h2><p>PIN, biometrics, trusted devices and account controls are part of the product roadmap.</p></div><ShieldCheck size={28}/></section><section className="section transactions"><div className="section-head"><div><p className="kicker">ACTIVITY</p><h2>Recent transactions</h2></div><button className="link" onClick={()=>setTab('Activity')}>See all</button></div>{txs.slice(0,4).map(tx=><Transaction key={tx.id} tx={tx} onClick={()=>setSelectedTx(tx)}/>)}</section></>}
{tab==='Wallet'&&<section className="page"><p className="kicker">WALLETS</p><h1>One account. Many currencies.</h1><p className="muted">Keep your money close, wherever it lives.</p><div className="wallet-list"><WalletRow code="NGN" name="Naira wallet" amount="₦1,247,850.00"/><WalletRow code="USD" name="US Dollar" amount="$804.25"/><WalletRow code="GBP" name="British Pound" amount="£120.00"/><WalletRow code="EUR" name="Euro" amount="€90.00"/></div><div className="action-grid"><button onClick={()=>setNotice('Add money is ready for a funding-provider connection.')}><Plus/>Add money</button><button onClick={()=>setNotice('Withdraw is ready for a provider connection.')}><ArrowUpRight/>Withdraw</button><button onClick={()=>openService('Multi-currency')}><Sparkles/>Convert</button><button onClick={()=>setShowWalletTools(true)}><ShieldCheck/>Safe & Spend</button></div></section>}
{tab==='Cards'&&<section className="page"><p className="kicker">CARDS</p><h1>Your BH'S card</h1><p className="muted">Control your virtual card before it controls your spend.</p><div className={`bank-card ${cardFrozen?'frozen':''}`}><div className="card-top"><span>BH'S</span><span>VISA</span></div><div className="chip">▦</div><div className="card-number">4821&nbsp;&nbsp; 9012&nbsp;&nbsp; 3418&nbsp;&nbsp; 2046</div><div className="card-bottom"><span>TAIWO B.</span><span>09/29</span></div>{cardFrozen&&<span className="frozen-badge">FROZEN</span>}</div><div className="card-tools"><button onClick={()=>{setCardFrozen(v=>!v);setNotice(cardFrozen?'Virtual card unfrozen in sandbox mode.':'Virtual card frozen in sandbox mode.');}}><ShieldCheck size={19}/> {cardFrozen?'Unfreeze card':'Freeze card'}</button><button onClick={()=>setNotice('Card spending and online-payment controls opened.')}><Settings size={19}/> Card controls</button></div><div className="setting-list"><div><span>Online payments</span><b>ON</b></div><div><span>Spending limit</span><b>₦250,000/day</b></div><div><span>Issuer</span><b>Provider connection required</b></div></div></section>}
{tab==='Activity'&&<section className="page"><p className="kicker">ACTIVITY</p><h1>Transaction history</h1><p className="muted">Search, filter and inspect every movement.</p><div className="search-box"><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search transactions"/></div><div className="filter-row">{['All','Money in','Money out'].map(filter=><button key={filter} className={activityFilter===filter?'active':''} onClick={()=>setActivityFilter(filter)}>{filter}</button>)}</div>{visibleTxs.length?visibleTxs.map(tx=><Transaction key={tx.id} tx={tx} onClick={()=>setSelectedTx(tx)}/>):<div className="empty"><ReceiptText size={25}/><strong>No matching transactions</strong><span>Try another search or filter.</span></div>}</section>}</main>
<nav className="bottom-nav">{[['Home','Home',Home],['Wallet','Wallet',Wallet],['Cards','Cards',CreditCard],['Activity','Activity',ReceiptText]].map(([id,label,Icon])=><button className={tab===id?'selected':''} key={label as string} onClick={()=>setTab(id as string)}><Icon size={20}/><span>{label}</span></button>)}<button onClick={()=>setShowMore(true)}><MoreHorizontal size={20}/><span>More</span></button></nav>
{showSend&&<div className="overlay"><div className="sheet"><div className="sheet-head"><div><p className="kicker">TRANSFER</p><h2>{transferStep==='form'?'Send money':transferStep==='review'?'Review transfer':'Transfer result'}</h2></div><button aria-label="Close transfer" onClick={closeSend}><X/></button></div>{transferStep==='form'&&<><label>Recipient<input autoFocus value={recipient} onChange={e=>setRecipient(e.target.value)} placeholder="Name, account or phone"/></label><label>Amount<input inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} placeholder={currency==='NGN'?'₦0.00':'$0.00'}/></label><div className="transfer-note"><ShieldCheck size={17}/><span>Secure sandbox transfer. No real funds are moved.</span></div>{notice&&<p className="form-error">{notice}</p>}<button className="primary wide" onClick={reviewTransfer}>Review transfer <ArrowUpRight size={17}/></button></>}{transferStep==='review'&&<><div className="review-card"><div><span>TO</span><strong>{recipient}</strong></div><div><span>AMOUNT</span><strong>{currency==='NGN'?'₦':'$'}{Number(amount).toLocaleString()}</strong></div><div><span>STATUS</span><strong>Sandbox · Ready to confirm</strong></div></div><div className="transfer-note"><ShieldCheck size={17}/><span>Review complete. This action only creates a sandbox record.</span></div><button className="primary wide" onClick={confirmTransfer}>Confirm sandbox transfer <Check size={17}/></button><button className="text-button" onClick={()=>setTransferStep('form')}>Edit transfer</button></>}{transferStep==='result'&&<div className="result-state"><span className="result-icon"><Check size={26}/></span><p className="kicker">{transferStatus==='completed'?'TRANSFER SUCCESSFUL':'TRANSFER PROCESSING'}</p><h2>{transferStatus==='completed'?'Money movement recorded.':'We are still processing.'}</h2><p className="muted">Sandbox only — no real funds were moved. Your activity has been updated for this demo.</p><button className="primary wide" onClick={closeSend}>Done</button></div>}</div></div>}
{serviceKind&&<ServiceFlow kind={serviceKind} onClose={()=>setServiceKind(null)} onComplete={completeService}/>} {showWalletTools&&<div className="overlay"><div className="sheet compact"><button className="close" onClick={()=>setShowWalletTools(false)}><X/></button><p className="kicker">SAFE & SPEND</p><h2>Separate your money.</h2><p className="muted">Move funds between your everyday Spend balance and protected Safe balance. Authentication will be required when live money movement is enabled.</p><div className="safe-balance"><div><span>SPEND</span><strong>₦247,850</strong></div><div><span>SAFE</span><strong>₦1,000,000</strong></div></div><button className="primary wide" onClick={()=>setNotice('Safe ↔ Spend transfer is a sandbox/future-provider action.')}>Move money</button></div></div>}
{showMore&&<div className="overlay"><div className="sheet compact"><button className="close" onClick={()=>setShowMore(false)}><X/></button><div className="profile-mark">{session.user.fullName.split(/\s+/).map(part=>part[0]).slice(0,2).join('').toUpperCase()}</div><p className="kicker">ACCOUNT</p><h2>Welcome, {session.user.fullName.split(/\s+/)[0]}.</h2><p className="muted">{session.user.email}</p><div className="menu-list"><button className="menu-row" onClick={()=>setNotice('Profile is ready for customer-account integration.')}><FileText/> Profile <ChevronRight/></button><button className="menu-row" onClick={()=>setNotice('Security centre opened: PIN, biometrics, devices and sessions.')}><ShieldCheck/> Security centre <ChevronRight/></button><button className="menu-row" onClick={()=>setNotice('KYC status: provider connection required.')}><Check/> KYC status <ChevronRight/></button><button className="menu-row" onClick={()=>setNotice('Support centre opened.')}><MoreHorizontal/> Help & support <ChevronRight/></button><button className="menu-row" onClick={()=>setNotice('Settings opened.')}><Settings/> Settings <ChevronRight/></button><button className="menu-row" onClick={signOut}><X/> Sign out <ChevronRight/></button></div></div></div>}
{selectedTx&&<div className="overlay"><div className="sheet compact"><button className="close" onClick={()=>setSelectedTx(null)}><X/></button><p className="kicker">TRANSACTION</p><h2>{selectedTx.title}</h2><div className="detail-amount">{selectedTx.amount}</div><div className="detail-grid"><div><span>Recipient / source</span><strong>{selectedTx.subtitle}</strong></div><div><span>Date</span><strong>{selectedTx.date}</strong></div><div><span>Status</span><strong className={`status ${selectedTx.status}`}>{selectedTx.status}</strong></div><div><span>Mode</span><strong>BH'S sandbox</strong></div></div><button className="outline wide" onClick={()=>setNotice('Receipt summary ready to share.')}>View receipt</button></div></div>}{notice&&!showSend&&!showMore&&!showWalletTools&&!selectedTx&&<button className="toast" onClick={()=>setNotice('')}>{notice}<X size={15}/></button>}</div>}
function Transaction({tx,onClick}:{tx:Tx;onClick:()=>void}){return <button className="tx" onClick={onClick}><div className="tx-icon">{tx.positive?<ArrowDownLeft size={18}/>:<ArrowUpRight size={18}/>}</div><div className="tx-copy"><strong>{tx.title}</strong><span>{tx.subtitle}</span></div><div className={tx.positive?'tx-amount positive':'tx-amount'}><strong>{tx.amount}</strong><span>{tx.date}</span><em className={`status-dot ${tx.status}`}>{tx.status}</em></div><ChevronRight size={16} className="tx-chevron"/></button>}
function WalletRow({code,name,amount}:{code:string;name:string;amount:string}){return <div className="wallet-row"><span className="currency">{code}</span><div><strong>{name}</strong><small>Available</small></div><strong>{amount}</strong></div>}
export default App;
